/**
 * Placeholder module retained so older imports don't leak secrets.
 * All Supabase configuration now flows through environment variables.
 */

export const projectId = '';
export const publicAnonKey = '';
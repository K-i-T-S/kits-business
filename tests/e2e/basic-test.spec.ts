// Superseded by smoke.spec.ts — kept as placeholder to avoid breaking imports
import { test, expect } from '@playwright/test';

test('placeholder — see smoke.spec.ts', () => {
  expect(true).toBe(true);
});
